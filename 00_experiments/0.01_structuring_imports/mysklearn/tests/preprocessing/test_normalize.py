from mysklearn.preprocessing.normalize import (
    find_max, find_min, normalize_data
)

# def test_find_max(x):
#     assert find_max([1, 4, 7, 1]) == 7
#
# def test_find_min(x):
#     assert find_max([1, 4, 7, 1]) == 1

